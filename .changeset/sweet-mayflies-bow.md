---
"@knime/eslint-config": major
---

update to eslint 9 and migrate eslint configs to flat config format

using this version will require you to also update to eslint 9.x.x and use the flat config format
