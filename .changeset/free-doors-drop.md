---
"@knime/components": minor
"@knime/utils": minor
---

Move `icons` and `isIconExisting` from @knime/utils to @knime/components and rename to `fileTypeIcons` and `isFileTypeIconExisting` respectively.
