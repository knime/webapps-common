# ![Image](https://www.knime.com/sites/default/files/knime_logo_github_40x40_4layers.png) @knime/ui-extension-renderer

Internal shared components and utilities to render UI-Extensions. Used in other consuming `@knime` packages.

## Installation

To install the `@knime/ui-extension-renderer` package, you can use npm:

```bash
npm install @knime/ui-extension-renderer
```

## Usage

To use it in your project, you can import it as follows:

```javascript
import { UIExtension } from "@knime/ui-extension-renderer";
```

or import types as follow:

```typescript
import type { ExtensionConfig } from "@knime/ui-extension-renderer";
```

# Join the Community!

- [KNIME Forum](https://forum.knime.com/)
